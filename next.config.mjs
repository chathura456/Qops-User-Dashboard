/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['www.thedevopsconference.com', 'cdn.geekwire.com', 'www.kreativdistrikt.com', 'calendar.boussiasevents.gr'],
    },
  };
  

export default nextConfig;
